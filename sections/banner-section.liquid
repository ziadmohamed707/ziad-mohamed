{% comment %}
Banner Section
- All text in red rectangles from Figma is now editable via theme customizer settings.
- Two distinct buttons with animation are implemented.
- Layout remains responsive and based on the Figma design.
- Pixel-approx layout; edit CSS to match Figma precisely
{% endcomment %}

<section id="custom-banner-{{ section.id }}" class="custom-banner-section" aria-label="Banner section">
  <div class="banner-inner">
    <!-- Header-like text, editable per the user's request -->
    <div class="banner-top-text">
        {{ section.settings.header_text }}
    </div>

    <!-- Main Content -->
    <div class="banner-content-container">
        <div class="banner-media" style="background-image: url({{ 'Banner.png' | asset_url }});"></div>
        <div class="banner-content">
            {% if section.settings.eyebrow_cta_text != blank %}
              <div class="banner-eyebrow">{{ section.settings.eyebrow_cta_text }}</div>
            {% endif %}
            
            {% if section.settings.cta_1_text != blank %}
              <a class="banner-cta" href="{{ section.settings.cta_1_url }}" target="{{ section.settings.cta_1_new_tab ? '_blank' : '_self' }}">
                <span class="cta-text">{{ section.settings.cta_1_text }}</span>
                <span class="cta-arrow" aria-hidden="true">→</span>
              </a>
            {% endif %}

            <h1 class="banner-title">{{ section.settings.title }}</h1>
            <p class="banner-sub">{{ section.settings.subtitle }}</p>

            {% if section.settings.cta_2_text != blank %}
              <a class="banner-cta" href="{{ section.settings.cta_2_url }}" target="{{ section.settings.cta_2_new_tab ? '_blank' : '_self' }}">
                <span class="cta-text">{{ section.settings.cta_2_text }}</span>
                <span class="cta-arrow" aria-hidden="true">→</span>
              </a>
            {% endif %}
        </div>
    </div>
    <!-- Small bottom text, editable per the user's request -->
    <div class="banner-bottom-text">
        {{ section.settings.bottom_text }}
    </div>

  </div>

  <style>
    /* Banner layout (self-contained) */
    #custom-banner-{{ section.id }} { 
      width: 100%; 
      display: block; 
      padding: 40px 0; 
      font-family: 'Inter', sans-serif;
    }
    #custom-banner-{{ section.id }} .banner-inner { 
      max-width: 1200px; 
      margin: 0 auto; 
      display: flex; 
      flex-direction: column;
      gap: 28px;
    }
    .banner-top-text {
        text-align: left;
        font-weight: bold;
        padding-left: 20px;
        text-transform: uppercase;
    }
    .banner-content-container {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 28px;
      align-items: center;
    }
    .banner-media { 
      background-size: cover; 
      background-position: center; 
      min-height: 550px; 
      border-radius: 2px; 
      box-shadow: 0 8px 30px rgba(0,0,0,.06);
      background-repeat: no-repeat;
    }
    .banner-content { 
      padding: 18px 10px; 
      display: flex;
      flex-direction: column;
      gap: 16px;
      position: relative;
    }
    .banner-eyebrow { 
      color: #777; 
      font-size: 13px; 
      text-transform: uppercase; 
      letter-spacing: .08em; 
    }
    .banner-title { 
      font-size: 40px; 
      margin: 0; 
      line-height: 1.05; 
    }
    .banner-sub { 
      color: #555; 
      margin: 0; 
    }
    .banner-cta { 
      display: inline-flex; 
      align-items: center; 
      gap: 10px; 
      padding: 12px 18px; 
      border-radius: 4px; 
      background: #111; 
      color: #fff; 
      text-decoration: none; 
      transform: translateY(6px); 
      opacity: 0; 
      animation: bannerCtaIn 0.6s 0.25s forwards; 
      max-width: fit-content;
    }
    .banner-cta:hover { 
      transform: translateY(0); 
      box-shadow: 0 6px 20px rgba(0,0,0,.12); 
    }
    .banner-cta .cta-arrow { 
      font-weight: 700; 
    }
    .banner-bottom-text {
        text-align: center;
        color: #777;
        font-size: 12px;
        letter-spacing: .05em;
        text-transform: uppercase;
    }
    @keyframes bannerCtaIn {
      to { transform: translateY(0); opacity:1; }
    }

    /* Responsive */
    @media (max-width: 820px) {
      #custom-banner-{{ section.id }} .banner-inner { 
        padding: 0 16px; 
      }
      .banner-top-text {
        text-align: center;
        padding-left: 0;
      }
      .banner-content-container {
        grid-template-columns: 1fr;
        gap: 16px;
      }
      .banner-title { 
        font-size: 30px; 
        text-align: center;
      }
      .banner-media { 
        width: 100%; 
        min-height: 200px; 
      }
      .banner-content {
        padding: 0;
        align-items: center;
        text-align: center;
      }
    }
  </style>

  {% schema %}
  {
    "name": "Custom Banner (Figma)",
    "settings": [
      {
        "id": "header_text",
        "type": "text",
        "label": "Top header text",
        "default": "TISSO VISON"
      },
      {
        "id": "eyebrow_cta_text",
        "type": "text",
        "label": "Eyebrow text",
        "default": "Find the ideal gift for your loved ones."
      },
      {
        "id": "cta_1_text",
        "type": "text",
        "label": "Top right button text",
        "default": "CHOOSE GIFT →"
      },
      {
        "id": "cta_1_url",
        "type": "url",
        "label": "Top right button link",
        "default": "/collections/all"
      },
      {
        "id": "cta_1_new_tab",
        "type": "checkbox",
        "label": "Open top CTA in new tab",
        "default": false
      },
      {
        "id": "title",
        "type": "text",
        "label": "Main title",
        "default": "The Gift Guide"
      },
      {
        "id": "subtitle",
        "type": "textarea",
        "label": "Main subtitle",
        "default": "Discover joy: Your Ultimate Holiday Gift Destination. Explore our curated selection and find the perfect gifts to delight your loved ones this holiday season."
      },
      {
        "id": "cta_2_text",
        "type": "text",
        "label": "Main button text",
        "default": "SHOP NOW →"
      },
      {
        "id": "cta_2_url",
        "type": "url",
        "label": "Main button link",
        "default": "/collections/all"
      },
      {
        "id": "cta_2_new_tab",
        "type": "checkbox",
        "label": "Open main CTA in new tab",
        "default": false
      },
      {
        "id": "bottom_text",
        "type": "text",
        "label": "Bottom text",
        "default": "SUSTAINABLE, ETHICALLY MADE CLOTHES IN SIZES XXS TO 6XL"
      }
    ],
    "presets": [
      {
        "name": "Banner (Figma)",
        "category": "Custom"
      }
    ]
  }
  {% endschema %}
</section>
